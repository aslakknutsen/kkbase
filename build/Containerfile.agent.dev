# Dev Containerfile - uses pre-built binary from local build
FROM registry.access.redhat.com/ubi9/ubi-minimal:latest

# Install ca-certificates for HTTPS connections (required for Gemini API)
RUN microdnf install -y ca-certificates && \
    microdnf clean all

WORKDIR /app

# Create non-root user BEFORE copying binary
RUN useradd -u 1000 -r -s /sbin/nologin kkbase && \
    chown kkbase:kkbase /app

# Copy pre-built binary with correct ownership and permissions directly
# (must be built first with: go build -o out/agent ./cmd/agent)
COPY --chown=kkbase:kkbase --chmod=755 out/agent .

# Switch to non-root user
USER 1000

# Expose HTTP port for webhooks and health checks
EXPOSE 8082

ENTRYPOINT ["./agent"]

